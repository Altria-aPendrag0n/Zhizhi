# v0.1 测试版发布计划（存档）

> 本文件存档 v0.1 测试版发布评审结论（2026-08-11）。
> **P0 阻塞项**：在 `docs/progress.json` 中逐项执行，本对话完成。
> **P1 / P2 项**：以下仅存档，暂不执行，待 v0.1 反馈收集后评估排期。

## P0（阻塞发布，本对话执行）

详见 `docs/progress.json`，共 6 项：

1. **P0-2** 应用内版本信息与「关于」入口
2. **P0-6** 用户侧全局错误收集
3. **P0-3** 内置反馈渠道（一键导出日志 + 反馈指引）
4. **P0-5** 构建可复现性（Embedding 模型下载脚本 + 构建前校验）
5. **P0-4** 首次启动体验（清空示例数据 + 欢迎引导）
6. **P0-1** 用户文档（快速上手 + 数据与隐私 + FAQ）

## P1（强烈建议）

> 状态更新（2026-08-11）：**自动更新已执行**（tauri-plugin-updater + GitHub Releases 静态托管，无需服务器），**反馈邮箱已执行**（「关于知枝」mailto 一键反馈至 `1074253861@qq.com`）。以下为其余待评估项。

1. **自动更新**：✅ 已执行。集成 tauri-updater，「检查更新」按钮（检查→下载→安装→重启），发版清单生成脚本 `scripts/release-latest.mjs`，流程见 [RUN.md](./RUN.md) §4.4。
2. **「新建项目」stub 处理**：[App.vue](../study-thread/src/App.vue) `handleProjectAdd()` 提示「新建项目功能即将上线」，需实现或移除入口。
3. **「认知地图（开发中）」假数据入口**：[LearningHubPage.vue](../study-thread/src/views/LearningHubPage.vue) 节点/连线为硬编码演示数据，建议 v0.1 隐藏入口或整页标注「敬请期待」。
4. **代码签名（Windows SmartScreen）**：未签名安装包会被 SmartScreen 拦截，分发文档需说明绕过方式，正式版前购买代码签名证书。
5. **安装包体积实测**：transformers.js + 本地模型（约 60MB）使安装包远超 RUN.md 标注的 ~5MB，需实测并修正 RUN.md 分发表。
6. **过时错误提示**：[App.vue](../study-thread/src/App.vue) Embedding 加载失败仍提示「请开启代理/VPN」，模型已本地打包，需改为真实失败原因提示。
7. **CHANGELOG / Release Notes**：建立版本迭代记录。
8. **根 README 补全 + LICENSE**：当前根 README 仅一行标题，缺 LICENSE 文件。
9. **Cargo 元数据**：[Cargo.toml](../study-thread/src-tauri/Cargo.toml) `description = "A Tauri App"`、`authors = ["you"]`，需改真实信息。

## P2（建议，暂不执行）

1. **API Key 存储安全**：明文存 localStorage（[settings.ts](../study-thread/src/stores/settings.ts)），beta 可接受，长期迁系统钥匙串，并在隐私文档中说明。
2. **数据备份提示**：文档中说明「复制 Vault 目录即备份」。
3. **无 API Key 引导**：多数功能依赖 LLM，设置页应有「未配置 API Key，以下功能不可用」提示。
4. **发布前质量门**：全量测试 + 手动冒烟测试记录。

## 执行状态记录

- 2026-08-11：评审完成，P0 开始执行（见 progress.json 逐项 pass 状态）。
- 2026-08-11：**P0 全部 6 项已完成**（版本信息 / 全局错误收集 / 反馈渠道 / 模型构建门禁 / 首启体验 / 用户文档），P1/P2 继续存档待后续排期。
- 2026-08-11：**P1 自动更新与反馈邮箱已执行**——自动更新集成 tauri-plugin-updater（「检查更新」按钮 + GitHub Releases 静态清单，无需服务器，见 RUN.md §4.4）；反馈邮箱接入「关于知枝」（mailto 一键反馈至 `1074253861@qq.com`，日志文件由用户拖入附件）。
- 2026-08-11：**P1 其余可落地项完成**——Cargo 元数据（作者 Asahiiiasd）、Embedding 失败提示去代理化、CHANGELOG、Apache-2.0 LICENSE、未签名 SmartScreen 说明、安装包体积实测（MSI 28.7MB / NSIS 27.6MB）。
- 2026-08-11：**v0.1 发版准备就绪**——签名密钥已生成（公钥已写入 `tauri.conf.json`，私钥存 `~/.tauri/zhizhi.key`），endpoints 指向 `Altria-aPendrag0n/Zhizhi`，带签名构建产物（安装包 + `.sig` + `latest.json`）已产出；**待通过 GitHub MCP 创建 Release 上传**（见 `docs/progress.json` 任务「V0.1 发版」）。
- 2026-08-11：**v0.1.0 Release 已发布**（`Altria-aPendrag0n/Zhizhi` v0.1.0，Latest）——上传产物：`_0.1.0_x64-setup.exe` + `.sig`、`_0.1.0_x64_zh-CN.msi` + `.sig`、`latest.json`。**注意**：GitHub 会剥离 Release 资产名中的非 ASCII 字符，本地文件名「知枝_0.1.0_x64-setup.exe」上传后存储名变为「_0.1.0_x64-setup.exe」，`latest.json` 的 URL 已同步修正（`scripts/release-latest.mjs` 已内置同名规范化）。验证：exe/latest.json/.sig 均公开可下载，签名有效，应用内「检查更新」即可升级。
- 2026-08-12：**v0.1.1 Release 已发布**（`Altria-aPendrag0n/Zhizhi` v0.1.1）——修复 + 体验优化迭代（划线相关 4 项修复、AI 思考过程持久化、新建会话占位、删除 Vault、会话边栏仓库驱动、幽灵 Vault 修复）。**注意**：签名密钥重新生成（`~/.tauri/zhizhi.key`，密码存 `~/.tauri/zhizhi.key.password`），`tauri.conf.json` 的 pubkey 已同步更新，**旧客户端 v0.1.0 无法自动更新到 v0.1.1（公钥变更），需重新安装**；v0.1.1 之后版本可正常自动更新。上传产物：`_0.1.1_x64-setup.exe` + `.sig`、`_0.1.1_x64_zh-CN.msi` + `.sig`、`latest.json`。验证：latest.json（version=0.1.1）与安装包公开可下载（HTTP 200）、签名与本地一致。
