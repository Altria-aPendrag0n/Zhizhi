# 更新日志 (Changelog)

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 约定与 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.1] - 2026-09-04

v0.3.1 版本：来源锚定（NotebookLM 式引用回溯）+ 引导模式（Khanmigo 式伴读教练）。

### 新增

**联网搜索子代理（可选）**
- 新增子代理设置（跟随主模型直连 / 自定义子代理模型）：开启后主模型通过 web_search 客户端工具把联网检索委托给子代理——主模型本身无需支持联网（如 GLM 可用 DeepSeek 联网）
- 官方 API 页新增**模型选择**：对话模型 / 图转文字模型 / 联网搜索子代理模型三个下拉（均由服务端 /v1/models 下发），图转文字与联网模型可单独指定、默认跟随对话模型；并提供「启用联网搜索子代理」开关
- 自定义模型页的联网搜索子代理仅保留 direct/custom 两档（官方子代理由官方 API 页管理，本页不再出现任何官方配置）
- 官方模式请求携带 X-Zhizhi-Purpose: web_search 头，由服务端网关路由到「用途=web_search」的上游渠道（服务端管理台渠道用途新增「联网搜索子代理」选项，'*' 通用渠道不参与联网请求）
- 联网搜索降级可见化：服务商不支持 web_search 工具时注入思考块提示，运行期内该通道跳过联网尝试避免多余往返
- 402 额度耗尽 / 401 鉴权失败 / 429 限流错误友好化为中文指引，替代裸 JSON

**来源锚定（引用回溯）**
- 回答中引用知识库来源（笔记 / 参考资料）处标注 `[n]` 角标，消息末尾附来源列表（标题 + 页码/章节）
- 提示词约定编号 + 前端校验双保险：编造/越界编号按普通文本保留，最坏「该标没标」，不会「错误指向」
- 点击角标弹出来源浮层（命中片段 + 页码区间 + 「打开原文」）：笔记跳笔记详情，参考资料按页查看原文（复用 read_reference 管线）
- 引用来源随会话持久化（`<!-- citations -->` 区块），重启后角标与来源列表可恢复；主会话与分支会话均支持

**引导模式（伴读教练）**
- 会话级开关（输入框上方「引导模式」按钮）+ 设置页「引导模式默认开启」全局默认
- 引导策略：诊断起点 → 小步讲解 → 检查点 → 让用户产出；用户明确要直接答案时立即切直讲（逃生约定，纯提示词实现）
- 开关状态写入会话 frontmatter，跨重启恢复；主会话与分支会话（SKILL.md `{guide_mode}` 段注入同一策略）均支持
- **开关即时持久化**：点击切换立即写回会话文件（不必等下一条消息），切走再切回、存在后台回答时均保持不变
- 引导式回答同样带 `[n]` 角标与来源列表，与来源锚定自然共存

### 修复

- ModelConfigPage 测试的 settings mock 补充 `saveSettings`（修复先前「一键切换官方/自定义」持久化引入的 2 个测试失败）

## [0.3.0] - 2026-09-04

v0.3 版本：学习计划 Agent + 复习时间轴 + 知枝官方账号与官方通道。

### 新增

**学习计划（AI 规划师）**
- plan-architect 规划师 Agent：访谈式对话策略（一次一问、每问带推荐答案、Vault 假设确认），收集目标、基础与每日可用时间后生成「目标 → 阶段 → 每日任务」学习计划
- 计划以 Markdown 落盘 `<Vault>/plans/`（frontmatter 为权威结构化数据，正文为可读计划书），支持手动修改与整体重新生成
- 学习地图新增计划视图：今日任务按每日容量动态顺延、勾勒写回、「开始学习」直达会话并回填任务关联、阶段与整体预计完成日
- 侧边栏学习 / 计划会话两组独立折叠；首页提供计划任务入口

**复习时间轴**
- 复习视图新增未来一周时间轴：7 天横向卡片、今天高亮、逾期归今天标注、条目折叠展开、点击直达笔记（回看原文不触发复习调度）

**知枝官方账号与官方通道（可选）**
- 邮箱验证码注册、用户名密码登录；「记住密码」重启自动续期，凭据（refresh_token / api_key）仅存 OS 钥匙串，access_token 仅内存
- 模型配置一键切换官方 / 自定义，徽标随真实通道联动并持久化；官方通道支持对话模型下拉选择（/v1/models），当前模型失效自动切换
- 「用户」与官方 API 合并为同一账号页；忘记密码重置、注销账号；套餐 / 额度展示

### 修复

- 官方 Key 缺失自动补发自愈：登录 / 恢复会话发现钥匙串无 Key 时自动补发，修复空凭据导致发消息失败跳设置页
- officialApiEnabled 与登录态强一致并即时持久化，修复重启后官方通道残留「使用中」空凭据
- 授予 dialog:allow-confirm 权限并将 window.confirm 改为 await，修复确认框失效（如直接删除不弹确认）
- 学习计划向导空态不渲染对话容器，修复首次进入无法输入的空框
- 设置子页面根类名与布局解耦，修复嵌套路由下布局错乱、侧边栏常驻不再消失

## [0.2.1] - 2026-08-15

v0.2.1：资料库文件管理优化 + 自动更新提示 + 流程图渲染改进。

### 新增

- 启动时自动检查更新，发现新版本时弹窗提示（抽取 updater 工具复用）

### 优化

- 资料库本地文件管理：会话文件命名升级为「稳定 id + 标题 slug」混合命名，id 前缀区分类型（`sess_`/`new_`/`branch_`/`review_`），文件名可读、结构清晰
- 笔记引用与会话树节点从绝对路径升级为稳定 id，修改会话标题不再影响笔记链接；旧文件（`branch-*`/`review-*`）仅兼容读取、不迁移
- 流程图渲染分层：简单流程用 ASCII 字符画、复杂流程用 Mermaid

### 修复

- 修复「关于知枝」区块未渲染（AboutSection 模板引用但缺失 import）
- 修复学习统计中复习会话按新命名前缀排除，避免误计入问答

## [0.2.0] - 2026-08-14

v0.2 版本：PDF 参考资料支持 + 大文档检索 + 复习系统增强。

### 新增

**PDF 参考资料**
- 参考资料存储升级为自包含文件夹结构（兼容旧扁平格式懒迁移）
- Rust 后端集成 `pdf_oxide` 新增 `extract_pdf_text` 解析命令
- 上传 PDF 后后台解析 + 解析状态机 + ReferenceCard 状态徽标与失败重试
- 大 PDF 按章节分块多向量索引与检索命中定位
- `read_reference` 按页读取/检索并注入页码提示

**大文档检索**
- 超大 Markdown 参考文档按章节分块多向量索引，避免整篇超限导致检索失效

**复习系统增强**
- 按目标笔记难度与掌握程度动态选择题型；一次复习去重并过滤低质量题，题数随笔记内容量浮动
- 新增「情景题」：AI 提供情境、用户在情境下作答，底层题型不变
- 复习作答输入防提示词注入（检测 + 净化 + SKILL 声明）
- 复习题全部答完后进入完成态：输入框替换为「结束复习 / 继续提问」两按钮；结束复习归档到「已复习会话」，继续提问则恢复输入框自由追问
- 退出复习页面自动归档到「已复习会话」；重新打开已归档会话保留输入框可继续提问
- 资料库「复习会话」支持关键词搜索与按指定日期筛选

### 修复

- AI 输出的框线字符画流程图渲染错位
- 会话体验四项修复：空白界面自动建会话、参考文件多选上传、用户消息与 AI 思考过程可复制

### 优化

- Chat 支持 Mermaid 代码块渲染为 SVG 流程图

## [0.1.1] - 2026-08-12

v0.1 测试版首次迭代更新（修复 + 体验优化）。

### 修复

- 修复加粗文本划线标记不显示——同一划线文本多次出现时按出现序号（occurrence）精确定位到用户实际划的位置
- 修复表格单元格内划线误选中表格格式——`isSelectionWithinSingleCell` 判定跨单元格才整表摘录
- 修复表格划线创建分支——划线文本为整张表格 Markdown 源码时按连续行块定位上下文，渲染后 DOM 整表包裹高亮
- 修复幽灵 Vault——失效路径不再静默打开，修复首页统计全为 0 的问题
- AI 思考过程持久化到会话 Markdown——以 `<!-- thinking -->` 区块保存，切换会话后思考过程不丢失

### 新增

- 新建对话后会话栏显示「新对话」占位——空白会话首条消息落盘前以占位条目承载
- 新增「删除 Vault」功能——先停文件监听再递归删除，修复新建资料库无法删除

### 优化

- 会话边栏改为仓库驱动——去除 localStorage 会话缓存，读取 vault 自动加载会话

### 文档

- README 大幅完善：产品介绍、核心功能（划线摘录、分支会话、原子笔记、复习系统等）、9 张功能展示截图、安装指引

## [0.1.0] - 2026-08-11

v0.1 测试版（Beta）首发。

### 新增

**核心学习流程**
- 学习会话：与 AI 流式对话（支持 Anthropic / OpenAI / DeepSeek / 通义千问 / 智谱 / Ollama），本地向量检索注入上下文（RAG）
- 划线摘录：把 AI 回答提炼为原子笔记（`[[wikilink]]` 双向链接形成知识网络）
- 分支追问：从任意消息/笔记分叉出深度追问分支（最多 3 层）
- Obsidian 兼容 Markdown Vault：笔记/会话为纯 Markdown 文件，可复制目录即备份

**复习系统**
- 间隔重复（SRS）调度：到期清单、掌握度 × 复习曲线难度信号
- 六类出题题型：选择题 / 判断题 / 填空 / 简答 / 簇关联 / 辩论（多轮状态机）
- AI 正误判定：标准答案 + 首行判定徽章，反馈按题型适配
- 复习会话流式出题、临时复用与清理

**学习地图与数据总览**
- 主界面数据总览：问答/复习/笔记按天聚合，GitHub 风格全年学习频率格子图
- 学习地图视图：复习入口 + 认知地图（演示数据，后续开发）

**桌面能力（v0.1 测试版基础设施）**
- 自动更新：设置页「关于知枝」→「检查更新」（tauri updater + GitHub Releases 静态清单）
- 反馈渠道：一键导出调试日志、复制反馈信息、邮件反馈至 `1074253861@qq.com`
- 全局错误收集：未处理异常与 Promise 拒绝自动写入日志系统
- 首次启动欢迎引导：测试版声明 + 建 Vault / 配 API Key / 开始对话三步
- 应用内版本信息与「关于知枝」区块

**本地优先架构**
- 内置离线 Embedding 模型（all-MiniLM-L6-v2 + onnxruntime wasm），完全离线可用
- 无账号、无云同步，数据全部存于本地 Vault

### 文档
- 新增[用户手册](docs/user-guide.md)：快速上手、数据与隐私、FAQ、反馈方式
- 新增[运行与打包指南](docs/RUN.md)、[Code Wiki](docs/code-wiki/README.md)、[发布计划](docs/v0.1-release-plan.md)
- 构建可复现：Embedding 模型下载脚本 + 构建前校验门禁

### 说明
- 测试版未购买代码签名证书，Windows 安装时可能出现 SmartScreen 提示，点击「更多信息 → 仍要运行」即可安装
- 「新建项目」入口暂为占位提示，后续版本实现
